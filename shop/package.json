{
  "name": "shop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently --kill-others 'npm start --prefix cart' 'npm start --prefix listing' 'npm start --prefix host'",
    "postinstall": "(cd cart && npm install); (cd listing && npm install); (cd host && npm install);"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
